# servers.yaml
servers:
  # Local stdio-based servers
  local:
    - name: "crypto_search"
      command: "python"
      args: ["/servers/crypto.py"]
      transport: "stdio"
      #env:
      #  WORKSPACE_DIR: "/tmp/mcp_workspace"   

    